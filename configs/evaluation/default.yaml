backend: default
# Default evaluation configuration.

# Metrics to compute after training experiment.
metrics:
  - accuracy
  - f1_macro
  - precision_macro
  - recall_macro

# Whether to compute a confusion matrix.
compute_confusion_matrix: true