# No-op raw augmentation configuration.
# This explicitly disables raw signal augmentation.

# Whether to enable raw augmentation.
enabled: false

# Backend identifier. 'none' means no augmentation is applied.
backend: none
