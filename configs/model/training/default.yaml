# Number of training epochs.
epochs: 30

# Batch size used during training.
batch_size: 16

# Learning rate for the optimizer.
learning_rate: 0.0005

# Optimizer name.
optimizer: adam

# Model reproducibility
random_state: 67
deterministic: True # by default = False