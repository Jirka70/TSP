defaults:
  - _self_
  - preprocessing: mne
  - epoching: default
  - split: default
  - augmentation: none
  - training: eegnet
  - evaluation: default
  - visualization: matplotlib
  - dataset: eegbci

mode: training

reproducibility:
  output_dir: ./outputs